import { IconSquarePipe } from './icon-square.pipe';

describe('IconSquarePipe', () => {
  it('create an instance', () => {
    const pipe = new IconSquarePipe();
    expect(pipe).toBeTruthy();
  });
});
